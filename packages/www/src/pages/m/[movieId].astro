---
import { actions } from "astro:actions";

const { movieId } = Astro.params;

if (!movieId) {
  throw new Error("NOT FOUND");
}

const { data } = await Astro.callAction(actions.movie.getById, {
  id: Number(movieId),
});
---

<div>Movie: {JSON.stringify(data)}</div>
